# Shell Quest profile -- intentionally minimal
# Source .bashrc for interactive login shells so quest hooks and prompt load.
if [ -n "$BASH_VERSION" ] && [ -f "$HOME/.bashrc" ]; then
    . "$HOME/.bashrc"
fi
